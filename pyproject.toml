[project]
name = "prl-pubsite"
version = "0.1.0"
description = "Simplified PRL publications website using Flask and bibtexparser"
requires-python = ">=3.8"
dependencies = [
    "Flask",
    "bibtexparser",
    "requests"
]